import { describe, it, expect } from "vitest";
import { ConnectedAccountSession } from "@/domain/entities/ConnectedAccountSession";

describe("ConnectedAccountSession", () => {
  it("has a test file", () => {
    expect(true).toBe(true);
  });
});
