import { describe, it, expect } from "vitest";
import {
  ListingAuditRuleError,
  CreateListingAuditRuleParams,
} from "@/domain/entities/ListingAuditRule";

describe("ListingAuditRule", () => {
  it("has a test file", () => {
    expect(true).toBe(true);
  });
});
