test(audit): pin token discipline + Field Manual §6 + SubmitButton contracts (round 34)

Source-string contract pin tests, mirroring the round 16-33 pattern. None
of these findings require a code change in this round; they are all
already fixed in earlier PRs. The pin tests prevent regression of the
design-system token contracts and the Field Manual §6 promises.

Audit: docs/UI-ACCESSIBILITY-AUDIT-2026-08-14.md

H-01: var(--font-family-code) is not a defined token. The 12 audit-listed
files all reference var(--font-mono) now; the test scans each for
`var(--font-family-code)` and asserts zero matches.

H-02: Card.module.css default .card has no box-shadow. Field Manual §5
pins the border as the elevation; the L7 fix runs through to the file.

H-03: globals.css does not lift every card on hover via a broad
`[class*="card"]:hover` selector. The actual selector is gone; the test
strips block comments (the prior-fix docstring mentions the selector
literally) and asserts the live CSS stays clean.

H-04: MobileNavToggle.module.css has no backdrop-filter. The design brief
bans glassmorphism; the H-04 fix kept the rgba dim.

H-05: AdminAuditLogTable uses the canonical ink scale (`--ink-900`,
`--ink-700`, `--ink-500`). The audit's undefined tokens (`--text`,
`--ink-800`, `--ink-400`) are not present.

H-06: SubmitButton.tsx imports Button.module.css and composes the
className from styles.* references. No legacy `btn btn-primary` string.

H-07: SubmitButton and SubmitButtonProps are exported from
`@/components/ui`. The barrel also re-exports the other UI primitives
called out in the audit (Toast, Skeleton, EmptyState, MobileNavToggle,
CommandPalette, PrintButton, RouteError).

H-14: SubmitButton.tsx line 1 is the double-quote `"use client";`
directive. No single-quote variant anywhere in the file.

H-15: ImpersonationBanner ships an sr-only `<h2>Impersonation active</h2>`
heading + role="status" + aria-live="polite".

H-16: QuizEditor.tsx `document.querySelector` calls live inside useEffect
or inside a named handler with a `typeof document` guard. The
hidden-input seed useEffect is pinned explicitly.

H-17: NavSidebar badge renders via the styles.badge CSS module class.
The badge block carries no inline hex color and no `style={...}`.

All 31 tests in the new round34 file pass. pnpm tsc --noEmit and
pnpm lint are clean.
